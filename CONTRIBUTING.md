# 贡献指南

感谢参与。本文说明三件事：如何反馈问题、如何提交改动、以及代码与文档的书写规范。

## 反馈问题

- 请使用仓库的 Issue 模板，并带上：版本号（镜像 tag 或「面板设置 → 关于」）、部署方式（Docker / 源码）、
  复现步骤、相关日志（「面板设置 → 日志」，或用 `docker logs` 取容器日志）。
- **安全漏洞不要开公开 Issue**，报告方式见 [SECURITY.md](SECURITY.md)。

## 提交改动

1. 从 `main` 切分支开发，完成后发 Pull Request。`main` 始终保持可运行。
2. 提交信息使用 [Conventional Commits](https://www.conventionalcommits.org/) 格式：
   `feat:` / `fix:` / `docs:` / `refactor:` / `perf:` / `test:` / `build:` / `ci:` / `chore:`。
   标题一句话说明"改了什么"，正文说明"为什么"（约束、复现条件、影响面）。
3. 面向用户的改动同时写进 [CHANGELOG.md](CHANGELOG.md) 的 `Unreleased` 一节。
4. 涉及设计取向的改动（协议取舍、数据结构、外部依赖、失败策略）在
   [docs/adr/](docs/adr/) 增加一条 ADR，与代码一起提交。
5. 本项目没有构建步骤、没有第三方运行时依赖，只使用 Node 内置模块。请保持这一前提；
   引入依赖需要在 PR 中说明理由。

## 代码规范

- 运行环境：Node.js ≥ 22（使用内置 `node:sqlite`，因此不需要外部数据库依赖）。
- 模块系统：CommonJS（`require` / `module.exports`）。
- 格式：2 空格缩进、单引号、语句结尾带分号。
- 注释：对外暴露的函数与模块使用 JSDoc 块注释；注释解释意图与约束，不复述代码本身。

## 文档与注释的书写规范

这套规范是可执行的：`npm run check` 会同时跑语法检查（`tools/check-syntax.js`）与文风检查
（`tools/check-style.js`），CI 在每次 PR 上运行。
理由很简单 —— 面向公众的仓库里，注释和文档要能被陌生人读懂，且不携带任何私人语境。

### 应当写

- **为什么这样做**：外部约束、协议要求、必须维持的不变量。
- **不显然的取舍**：为什么选 A 不选 B，B 会在什么条件下失效。
- **边界与失败**：什么输入会走到哪条分支，失败时对外表现是什么。
- **契约**：字段含义、错误语义、上游/下游的假设。

### 不应写

| 类别 | 说明 |
|---|---|
| 人称与对话 | 不使用"我 / 我们 / 你"作为叙述主语；不引用对话内容（"（原话：…）"）。 |
| 私人语境 | 不写"某人要求 / 谁拍板 / 谁报的问题"。决策只记结论与理由。 |
| 自我评价与情绪 | "踩过坑 / 走眼了 / 说错了 / 真凶 / 要命"一类表述一律不写。 |
| 时间流水账 | 注释里不留"某年某月某日"式的过程记录。版本历史进 CHANGELOG，设计决策进 ADR。 |
| 口语化与感叹 | "其实 / 说白了 / 当然 / 竟然 / 别指望"等；命令行输出以外的符号（✔ ✘ ✅ ❌）不用在文档里。 |
| 噪声标记 | `⚠️` 只保留在"照做会出错"的位置；同一段落不重复出现。 |

### 术语表

| 不写 | 写 |
|---|---|
| 使用者 | 用户 |
| 拍板 / 定了 / 按他的办 | 决策（并在 ADR 中记录） |
| 实测踩过 | 已知边界（附复现条件） |
| 我加了一版被否掉 | 已否决方案（写进 ADR 的"备选方案"一节） |
| 猫源（作为产品名） | 猫爪源 / CatPawOpen 源（作为源类型名可保留） |

### 示例

改写前：

```js
 * ## 为什么换掉 TMDB 反查（2026-09-21 使用者定）
 * 原来是"精确同名，不行就拿候选名去 TMDB 反查 tmdbId"。实测坏在**喂进去的名字**上：
 * 源标题常写成「斗破苍穹年番4更211[2025][动漫]」，这种拿去 TMDB 当然查不到 ⇒ 同名 0、回退 0
 * ⇒ 没有版本（Emby 里表现为"条目在、点开没版本"）。
```

改写后：

```js
 * ## 设计取舍：为什么不用 TMDB 反查
 * 早期实现为"名字完全相等，否则将候选名交给 TMDB 反查 tmdbId"。失败点在输入侧：站源标题通常
 * 含更新话术与画质标注（如「…4更211[2025][动漫]」），直接用于检索无法命中，既得不到同名条目、
 * 也得不到别名命中，最终不返回任何线路。详见 ADR-0003。
```

## 文档结构

| 文件 | 面向 |
|---|---|
| [README.md](README.md) | 使用者：安装、首次使用、常见问题 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 开发者：分层、依赖方向、数据流 |
| [docs/](docs/index.md) | 开发者：分模块的详细文档 |
| [docs/adr/](docs/adr/) | 设计决策记录 |
| [CHANGELOG.md](CHANGELOG.md) | 版本变更 |

## 许可

提交即表示同意以本项目的许可证（见 [LICENSE](LICENSE)）发布你的贡献。
